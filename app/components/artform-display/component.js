import Ember from 'ember';

export default Ember.Component.extend({
  // portfolioItems: Ember.computed.uniqBy('items', 'portfolio'),
  // navItems: Ember.computed.filterBy('portfolioItems', 'artform', 'parent'),
});
